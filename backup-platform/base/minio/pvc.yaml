apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-pvc
  namespace: openshift-adp
spec:
  storageClassName: standard   # overridden by vsphere overlay → thin-csi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
